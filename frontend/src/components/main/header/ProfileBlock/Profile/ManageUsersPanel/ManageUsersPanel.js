import React from 'react';
import s from './ManageUsersPanel.module.css'
import {connect} from "react-redux";

const ManageUsersPanel = () => {
    return (
        <div className={s.manageUsersPanel}>
            
        </div>
    );
};

export default connect()(ManageUsersPanel);